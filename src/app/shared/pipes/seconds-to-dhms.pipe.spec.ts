import { SecondsToDhmsPipe } from './seconds-to-dhms.pipe';

describe('SecondsToDhmsPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToDhmsPipe();
    expect(pipe).toBeTruthy();
  });
});
